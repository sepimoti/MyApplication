<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/paint_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/paint_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".KotlinClassActivity"></activity>
        <activity android:name=".KotlinActivity" />
        <activity android:name=".DiceActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ConstraintLayoutActivity" />
        <activity android:name=".LinearLayoutActivity" />
        <activity android:name=".DigiKala" />
        <activity android:name=".RealativeLayoutActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>